# PRD Tracker

Source PRD: `docs/plans/2026-03-03-klip-prd.md`  
Last Updated: 2026-03-07

Status legend: `todo` | `in-progress` | `done`

| User Story | Title | Status | Evidence |
| --- | --- | --- | --- |
| US-001 | Clipboard text monitoring | in-progress | Frontend monitor dedup path in `src/features/history/clipboardMonitor.ts`; fixed timer binding for Tauri window runtime and expanded regression coverage in `tests/clipboardMonitor.test.ts`; added desktop-native clipboard read/write commands (`src-tauri/src/clipboard.rs`) and runtime port bridge (`src/features/history/browserClipboard.ts` + `src-tauri/src/lib.rs`); added desktop event-driven listener baseline via `clipboard-rs` watcher with `klip://clipboard-updated` runtime event bridge (`src-tauri/src/clipboard_listener.rs`, `src-tauri/src/lib.rs`) and frontend subscription with polling fallback (`src/features/history/browserClipboard.ts`, `src/features/history/clipboardMonitor.ts`); manual verification matrix now includes event-driven latency/hide-reopen scenarios and latest alias-baseline smoke evidence (`docs/status/manual-verification-us001-us006.md`, `/tmp/klip-dev-desktop-alias-20260306.log`); interactive macOS/Windows reliability evidence is still pending. |
| US-002 | History persistence and capacity | in-progress | Implemented in history storage/repository and settings-center max-items control in `src/App.tsx`; cross-platform runtime config hardening still pending. |
| US-003 | Tray/Menu bar entry | done | Implemented Tauri runtime tray/menu bar flow in `src-tauri/src/tray.rs` + `src-tauri/src/lib.rs` (icon click open panel, menu open panel, quit action, close-to-hide behavior). |
| US-004 | Global hotkey panel toggle | in-progress | Implemented in commit `30a6501`: Tauri global shortcut registration/rebind/conflict feedback in `src-tauri/src/hotkey.rs`; settings-center hotkey apply flow now in `src/App.tsx` + `src/features/settings`; default hotkey updated to `CommandOrControl+Shift+V`, and display/persistence/input/runtime-return paths now normalize runtime values into readable/canonical forms (`Cmd/Ctrl+Shift+V` and `CommandOrControl+Shift+V`) with legacy default migration from `...+K`; parser is now token-based (not `+`-separator dependent) to handle runtime variants like `shift super KeyV`; manual verification scenarios (open/focus/conflict/esc) are defined in `docs/status/manual-verification-us001-us006.md`; interactive macOS/Windows evidence is still pending. |
| US-005 | History search and keyboard navigation | in-progress | Keyboard navigation now supports Clipy-style cascading popup hierarchy in `src/App.tsx` (`↑/↓` move with non-selectable rows skipped, `←/→` level switch, `Enter` select/paste), and history default presentation is capped to 30 with grouped ranges at root (`src/features/menu/popupMenuModel.ts`, `src/features/history/history.constants.ts`); popup menu now includes a real-time search input and query-mode flattened results for both history/snippets (directly selectable without entering submenus), plus explicit empty-state labels for unmatched queries, with keyboard navigation from the search box (`src/App.tsx`, `src/features/menu/popupMenuModel.ts`, `src/styles.css`, `tests/popupMenuModel.test.ts`); browser Playwright E2E now verifies query search, empty-state, `Enter` copy fallback, and submenu keyboard open/close traversal for history in preview mode (`playwright.config.ts`, `tests/e2e/browser-preview.spec.ts`, `tests/e2e/fixtures.ts`); interactive desktop performance and cross-platform matrix evidence still pending. |
| US-006 | Direct paste on selection | in-progress | Added in commit `30a6501`: Tauri direct paste command with platform paste attempt + clipboard fallback (`src-tauri/src/direct_paste.rs`) and wired selection flow in `src/App.tsx`; added paste-mode toggle (`direct-with-fallback`/`clipboard-only`) for reliability testing in `src/features/settings/pasteModeStorage.ts`; manual verification scenarios (success auto-hide + failure fallback) are defined in `docs/status/manual-verification-us001-us006.md`; cross-platform interactive reliability verification is still pending. |
| US-007 | Snippets and folders CRUD | in-progress | Snippet/folder repository + persistence + UI CRUD implemented in `src/features/snippets` and `src/App.tsx`; optional snippet alias metadata is now supported with normalization + persistence path (`src/features/snippets/snippetRepository.ts`, `src/features/snippets/snippetUtils.ts`), and editor UI supports alias input + conflict guard (`src/App.tsx`); browser Playwright E2E now verifies inline snippet creation/persistence, snippet delete confirm, folder create/rename/delete with move-to-General behavior, folder rename conflict handling, alias-conflict rejection, and invalid-alias rejection in preview mode (`playwright.config.ts`, `tests/e2e/browser-preview.spec.ts`, `tests/e2e/fixtures.ts`); desktop manual verification is still pending. |
| US-008 | Snippets quick paste | in-progress | Snippet selection reuses direct paste runtime with clipboard fallback (`src/features/paste`, `src/App.tsx`); popup query mode now supports `;alias` lookup with flattened direct-select results and alias ranking (`src/features/menu/popupMenuModel.ts`, `src/App.tsx`), enabling faster keyboard paste in compact menu; global standalone alias trigger baseline is now implemented via dedicated alias hotkey registration + popup `;` prefill/focus event flow (`src/features/settings/hotkeyStorage.ts`, `src/features/settings/hotkeyRuntime.ts`, `src-tauri/src/hotkey.rs`, `src-tauri/src/lib.rs`, `src/App.tsx`); browser Playwright E2E now verifies snippet create → `;alias` search → `Enter` clipboard fallback in preview mode (`playwright.config.ts`, `tests/e2e/browser-preview.spec.ts`, `tests/e2e/fixtures.ts`); manual verification matrix includes US-008 alias quick-search scenarios (`docs/status/manual-verification-us001-us006.md`), but cross-platform interactive verification is still pending for native hotkey/direct-paste behavior. |
| US-009 | Dual-mode History/Snippets entry | in-progress | Popup root hierarchy is now flattened to Clipy-style sections (`历史`/`片断`) without `History`/`Snippets` wrapper nodes, with actions `清除历史/编辑片断.../偏好设置.../退出 Klip` and cascading submenu preview behavior implemented in `src/App.tsx` + `src/features/menu/popupMenuModel.ts`; popup shell now fills the window, menu mode remains undecorated at config level, and popup window size follows rendered content via `ResizeObserver` + runtime `setSize` (`src/styles.css`, `src/App.tsx`, `src-tauri/tauri.conf.json`); popup behavior uses runtime-measured stable column heights (session max), stable preview/list gutters, and visible bounding-box window sizing to avoid hover-time jitter/clipping regressions, and the stable-height baseline is now decoupled from hover-state transitions by measuring root-column content only on root-entry changes (`src/App.tsx`, `src/styles.css`); responsive-rule root-cause fix now scopes `@media (max-width: 760px)` management styles to `.app-shell-expanded` and removes popup row override so hover-triggered width changes no longer shrink popup history rows (`src/styles.css`); strict three-window runtime split is implemented via dedicated Tauri commands and window-role-aware frontend (`src-tauri/src/commands.rs`, `src-tauri/src/lib.rs`, `src/features/settings/hotkeyRuntime.ts`, `src/App.tsx`) so `编辑片断...` / `偏好设置...` open/focus independent windows and hide popup; snippet editor/preferences now have Clipy-style visual baseline with select-control style unification across windows (`src/App.tsx`, `src/styles.css`); browser Playwright E2E now verifies popup clear-history confirm/accept behavior, empty-history action messaging, submenu traversal, and browser-preview quit-action handling in preview mode (`tests/e2e/browser-preview.spec.ts`, `tests/e2e/fixtures.ts`); remaining gap is interactive/manual verification evidence and pixel-diff parity review. |
| US-010 | Settings center | in-progress | Preferences now open in a standalone `preferences` window (reuse/focus lifecycle) from popup action, while settings state persists and syncs across windows (`src-tauri/src/commands.rs`, `src/App.tsx`); settings UI follows Clipy-style icon-tab navigation and sectioned content layout with existing Klip settings wired into tabs, top-level tabs are constrained to a fixed single row (7 primary tabs) for parity in the `480x374` window, visual-scale tuning has been applied to tab/icon sizes plus content typography spacing, and menu-tab controls now use unified custom select styling (no native metallic gradient) for consistent UI language (`src/App.tsx`, `src/styles.css`); settings hotkey tab now supports independent snippet-alias global hotkey configuration (including disable via empty value) with runtime registration bridge (`src/features/settings/hotkeyStorage.ts`, `src/features/settings/hotkeyRuntime.ts`, `src/App.tsx`); browser Playwright E2E now verifies inline settings navigation plus max-history/paste-mode persistence across reloads in preview mode (`playwright.config.ts`, `tests/e2e/browser-preview.spec.ts`, `tests/e2e/fixtures.ts`); detailed design remains in `docs/plans/2026-03-04-three-window-clipy-parity-design.md`; remaining work is manual verification across popup transition/focus lifecycle and full screenshot-level parity checks. |
| US-011 | Cross-platform packaging verification | in-progress | Added packaging baseline scripts (`build:desktop:bundle:*`) and enabled bundle mode in Tauri config (`src-tauri/tauri.conf.json`); added macOS/Windows packaging CI matrix workflow (`.github/workflows/desktop-packaging.yml`); tag-triggered runs now publish GitHub Releases with checked-in release notes template (`.github/release-notes-template.md`) plus Windows `.exe` and macOS `.dmg` bundles, while `workflow_dispatch` remains artifact-only; macOS tag builds now support unsigned, signed-only, or signed-and-notarized paths depending on configured Apple certificate / App Store Connect secrets (`.github/workflows/desktop-packaging.yml`, `package.json`, `README.md`, `docs/status/packaging-verification-us011.md`); local macOS bundling now produces `Klip.app` and `Klip_0.1.0_aarch64.dmg`; a real signed/notarized tag run plus Windows artifact and interactive install/uninstall evidence still pending. |
| US-012 | Regression baseline and automation | in-progress | Unit tests and coverage gates exist; added popup menu model coverage (`tests/popupMenuModel.test.ts`) for history grouping and submenu path resolution; added popup responsive-scope guard test to prevent `@media (max-width: 760px)` from reintroducing popup selector leakage (`tests/popupResponsiveScope.test.ts`); added clipboard monitor subscription coverage for desktop event-driven path (`tests/clipboardMonitor.test.ts`); popup-search follow-up added additional popup model query coverage including query-mode flattened history/snippet results and `;alias` lookup path (`tests/popupMenuModel.test.ts`), while snippet domain tests now cover alias normalization/persistence/search (`tests/snippetUtils.test.ts`, `tests/snippetStorage.test.ts`, `tests/snippetRepository.test.ts`), and snippet-alias-hotkey storage coverage validates default/normalize/disable behavior (`tests/snippetAliasHotkeyStorage.test.ts`); browser Playwright baseline is now wired through `playwright.config.ts` with seeded preview fixtures and thirteen E2E scenarios covering popup/menu navigation, history query copy fallback, snippet alias flow, settings persistence, clear-history confirm + empty-history action messaging, alias-conflict rejection, submenu keyboard traversal, snippet delete confirm, folder CRUD/relocation, folder rename conflict handling, invalid-alias rejection, and browser-preview quit handling (`tests/e2e/browser-preview.spec.ts`, `tests/e2e/fixtures.ts`), while CI now installs Playwright Chromium before `npm run test:e2e` (`.github/workflows/ci.yml`); latest local validation passed via `npm run qa` (`test` 85, `test:e2e` 13, coverage statements `85.24%`, branches `86.94%`, funcs `84.67%`, lines `85.24%`, `cargo:check` pass); desktop smoke was previously revalidated on event-driven baseline (`/tmp/klip-dev-desktop-event-listener-20260306.log`), and native/manual verification still remains for tray/hotkey/direct-paste/window-lifecycle behavior; CI install reliability hardening, registry normalization, cross-platform LF/Rust upgrades, Windows icon asset, and commit-governance enforcement remain in place (`.github/workflows/ci.yml`, `.npmrc`, `package-lock.json`, `src-tauri/icons/icon.png`, `src-tauri/icons/icon.ico`, `AGENTS.md`, `.commitlintrc.json`). |

## Immediate Next Stories

1. Run macOS interactive verification for popup hierarchy/focus flow, three-window reuse behavior, and event-driven clipboard capture.
2. Complete screenshot-level parity review for `snippet_edit.png` and `settings*.png`.
3. Provision Apple release secrets, run a tag-based US-011 release dry run, and continue Windows packaging evidence (GitHub Release asset upload + install/uninstall matrix).
