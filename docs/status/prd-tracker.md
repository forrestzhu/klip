# PRD Tracker

Source PRD: `docs/plans/2026-03-03-klip-prd.md`  
Last Updated: 2026-03-04

Status legend: `todo` | `in-progress` | `done`

| User Story | Title | Status | Evidence |
| --- | --- | --- | --- |
| US-001 | Clipboard text monitoring | in-progress | Frontend polling monitor + dedup in `src/features/history/clipboardMonitor.ts`; fixed timer binding for Tauri window runtime and added regression test in `tests/clipboardMonitor.test.ts`; added desktop-native clipboard read/write commands (`src-tauri/src/clipboard.rs`) and runtime port bridge (`src/features/history/browserClipboard.ts` + `src-tauri/src/lib.rs`); manual verification matrix and local preflight evidence added in `docs/status/manual-verification-us001-us006.md`; event-driven listener integration and interactive macOS/Windows verification are still pending. |
| US-002 | History persistence and capacity | in-progress | Implemented in history storage/repository and settings-center max-items control in `src/App.tsx`; cross-platform runtime config hardening still pending. |
| US-003 | Tray/Menu bar entry | done | Implemented Tauri runtime tray/menu bar flow in `src-tauri/src/tray.rs` + `src-tauri/src/lib.rs` (icon click open panel, menu open panel, quit action, close-to-hide behavior). |
| US-004 | Global hotkey panel toggle | in-progress | Implemented in commit `30a6501`: Tauri global shortcut registration/rebind/conflict feedback in `src-tauri/src/hotkey.rs`; settings-center hotkey apply flow now in `src/App.tsx` + `src/features/settings`; default hotkey updated to `CommandOrControl+Shift+V`, and display/persistence/input/runtime-return paths now normalize runtime values into readable/canonical forms (`Cmd/Ctrl+Shift+V` and `CommandOrControl+Shift+V`) with legacy default migration from `...+K`; parser is now token-based (not `+`-separator dependent) to handle runtime variants like `shift super KeyV`; manual verification scenarios (open/focus/conflict/esc) are defined in `docs/status/manual-verification-us001-us006.md`; interactive macOS/Windows evidence is still pending. |
| US-005 | History search and keyboard navigation | in-progress | Keyboard navigation now supports Clipy-style cascading popup hierarchy in `src/App.tsx` (`↑/↓` move with non-selectable rows skipped, `←/→` level switch, `Enter` select/paste), and history default presentation is capped to 30 with grouped ranges at root (`src/features/menu/popupMenuModel.ts`, `src/features/history/history.constants.ts`); interactive performance and cross-platform matrix evidence still pending. |
| US-006 | Direct paste on selection | in-progress | Added in commit `30a6501`: Tauri direct paste command with platform paste attempt + clipboard fallback (`src-tauri/src/direct_paste.rs`) and wired selection flow in `src/App.tsx`; added paste-mode toggle (`direct-with-fallback`/`clipboard-only`) for reliability testing in `src/features/settings/pasteModeStorage.ts`; manual verification scenarios (success auto-hide + failure fallback) are defined in `docs/status/manual-verification-us001-us006.md`; cross-platform interactive reliability verification is still pending. |
| US-007 | Snippets and folders CRUD | in-progress | Snippet/folder repository + persistence + UI CRUD implemented in `src/features/snippets` and `src/App.tsx`; conflict/UX hardening pending. |
| US-008 | Snippets quick paste | in-progress | Snippet selection reuses direct paste runtime with clipboard fallback (`src/features/paste`, `src/App.tsx`); global trigger/alias and cross-platform verification are still pending. |
| US-009 | Dual-mode History/Snippets entry | in-progress | Popup root hierarchy is now flattened to Clipy-style sections (`历史`/`片断`) without `History`/`Snippets` wrapper nodes, with actions `清除历史/编辑片断.../偏好设置.../退出 Klip` and cascading submenu preview behavior implemented in `src/App.tsx` + `src/features/menu/popupMenuModel.ts`; popup shell now fills the window, menu mode remains undecorated at config level, and popup window size follows rendered content via `ResizeObserver` + runtime `setSize` (`src/styles.css`, `src/App.tsx`, `src-tauri/tauri.conf.json`); main-window runtime capability config was added in `src-tauri/capabilities/default.json`; local popup badcase screenshots were captured in `docs/klip-test-ui` for visual verification evidence; remaining gap is strict three-window separation for snippet editor/preferences windows. |
| US-010 | Settings center | in-progress | Current settings are available but still embedded in app panel flow; target is independent Preferences window as part of three-window model. Detailed design and transition rules are documented in `docs/plans/2026-03-04-three-window-clipy-parity-design.md`; implementation and manual verification are pending. |
| US-011 | Cross-platform packaging verification | in-progress | Added packaging baseline scripts (`build:desktop:bundle:*`) and enabled bundle mode in Tauri config (`src-tauri/tauri.conf.json`); added macOS/Windows packaging CI matrix workflow (`.github/workflows/desktop-packaging.yml`); added packaging checklist + install/run/uninstall matrix + release-note draft (`docs/status/packaging-verification-us011.md`); local macOS bundling now produces `Klip.app` and `Klip_0.1.0_aarch64.dmg`; Windows artifact and interactive install/uninstall evidence still pending. |
| US-012 | Regression baseline and automation | in-progress | Unit tests and coverage gates exist; added popup menu model coverage (`tests/popupMenuModel.test.ts`) for history grouping and submenu path resolution; desktop smoke path revalidated after popup/editor split (`/tmp/klip-dev-desktop-menu-rework-20260304.log`); latest `npm run qa` passed with 68 tests and coverage statements `87.62%`; CI install reliability hardening added for recurring `npm ci` internal crash by pinning Node `22.22.0`/npm `10.9.4`, adding `npm ci` retry + cache clean, disabling Husky during CI install, and printing npm debug logs on terminal failure (`.github/workflows/ci.yml`); follow-up root-cause fix added by normalizing lockfile tarball hosts from `registry.anpm.alibaba-inc.com` to `registry.npmjs.org`, pinning repo registry via `.npmrc`, and explicitly setting npmjs registry in CI setup/install path (`package-lock.json`, `.npmrc`, `.github/workflows/ci.yml`); additional CI stabilization added for post-npm blockers by forcing LF checkout with `.gitattributes` (fixes Windows lint CRLF diffs), upgrading Rust toolchain to `1.85.0` in repo/workflow (fixes Ubuntu cargo parse error for `edition2024` transitive crates), adding Linux GTK/WebKit dependency install for Ubuntu cargo checks, and providing required Windows `.ico` icon asset for Tauri build scripts (`.github/workflows/ci.yml`, `src-tauri/icons/icon.png`, `src-tauri/icons/icon.ico`). |

## Immediate Next Stories

1. Implement strict three-window behavior by splitting `编辑片断...` and `偏好设置...` into independent windows with reuse/focus lifecycle.
2. Align snippet editor and preferences visuals to `docs/clipy_ui/snippet_edit.png` and `docs/clipy_ui/settings*.png`.
3. Run macOS interactive verification for popup hierarchy/focus flow, then continue US-011 Windows packaging evidence.
